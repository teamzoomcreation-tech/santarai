/**
 * Génération de projet — même logique que le Conductor.
 * Facturation : totalCost = newAgents.length * AGENT_COST (15 000 TK/agent).
 * Si trésorerie insuffisante → 402 Payment Required.
 */

export { POST } from "@/app/api/conductor/route"
